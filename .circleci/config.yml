version: 2
jobs:
  build:
    docker:
      - image: php:7.0-apache
    steps:
      - checkout
      - setup_remote_docker
      - run: docker build -t mfieldhouse/docker-php-helloworld:$CIRCLE_BRANCH .


